// Loading.js
import React, { FC } from "react";

const Loading: FC = () => {
    return (
        <h3 className="font-mono text-2xl mt-5 ml-5">Загрузка данных...</h3>
    );
};

export default Loading;
